name: Build, Lint, Test, Docs Next

on:
  pull_request:
    branches-ignore:
      - main

jobs:
  dist:
    runs-on: ubuntu-latest

    services:
      ipfs:
        image: ipfs/kubo:latest
        ports:
          - 4001:4001
          - 4001:4001/udp
          - 8080:8080
          - 5001:5001

      localstack:
        image: localstack/localstack
        ports:
          - 4566:4566
          - 4571:4571
        env:
          DEFAULT_REGION: eu-central-1
          AWS_ACCESS_KEY_ID: test
          AWS_SECRET_ACCESS_KEY: test
          SERVICES: s3

    steps:
      - name: Checkout
        uses: actions/checkout@v4

      - name: Setup Node
        uses: actions/setup-node@v4
        with:
          node-version: 20

      - name: Check LocalStack logs
        run: docker logs localstack

      - name: Verify LocalStack container ports
        run: docker ps

      - name: Wait for LocalStack to be ready
        run: |
          until curl -s curl http://localhost:4566/_localstack/health | grep '"s3": "running"'; do
            echo "Waiting for LocalStack..."
            sleep 5
          done

      - name: Restore root node_modules from cache
        id: node-cache
        uses: actions/cache@v4
        with:
          path: node_modules
          key: node-modules-${{ hashFiles('package-lock.json') }}

      - name: Install dependencies
        run: npm ci

      - name: Run Lint and Dist
        run: |
          npm run lint
          npm run dist